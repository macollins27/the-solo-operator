---
# ====================================================================
# Phrasebook entry template
# ====================================================================
# Copy this file to <slug>.md (drop the .txt extension) and fill in
# every field. The Zod schema in src/content/config.ts validates this
# shape at build time; missing or malformed fields will fail `pnpm build`.
#
# Sterilization rule: NEVER include client names, project names,
# session IDs, work-substance descriptions, or dollar amounts. Stories
# must use generic framing — "a service," "a small company," etc.
# See site/CLAUDE.md § Sterilization and § Phrasebook story mode.
# ====================================================================

# The page title — declarative, names the agent's behavior.
# Format: "Your agent says X" or "Your agent does Y"
# Max 120 chars.
title: "Your agent says X"

# The literal phrase the agent emits. Renders italicized in the H1.
# Lowercase first letter; no trailing period (the template adds one).
# Max 120 chars.
quote: "the agent's exact words go here"

# Three short sentences. First disagrees, second instructs, third reassures.
# Pattern from the published entry: "It probably doesn't. Push back once. The free path usually exists."
# Max 180 chars.
subtitle: "Three short sentences. First disagrees. Second instructs. Third reassures."

# SEO meta description. Appears in search results.
# Max 180 chars.
description: "What's happening, what to do, in one sentence the reader can skim."

# One of: giving-up, done-before-done, handing-back, making-up.
# See src/lib/categories.ts for labels.
category: giving-up

# Aggregate count from corpus. Format: "~38 sessions" or "103 sessions".
# Must match regex: /^~?\d[\d,]* sessions$/
appears: "~XX sessions"

# Total for the category. Same format.
categoryTotal: "~XXX sessions"

# Optional. Pattern: "~74%" or "82%". Approximate first-push success rate.
successRate: "~XX%"

# Optional. Links the entry to a foundation move.
# slug must be one of the five FOUNDATION_MOVE_SLUGS in src/lib/categories.ts.
relatedMove:
  slug: push-back-on-i-cant
  label: "Push back on 'I can't' once"

# 3-5 phrases the agent might actually emit, in the agent's voice.
# Each entry should be in agent-default register (apologetic, hedging, etc).
# Include at least one register-tic example ("Unfortunately, ...").
# Max 220 chars per entry.
hearing:
  - "First phrase in the agent's voice."
  - "Second phrase, different register tic."
  - "Third phrase, the canonical version."
  - "Fourth phrase, the apologetic-hedge version."

# THE primary action. The exact words to say back.
# Six imperatives or fewer. No "could you", no "please", no "try to".
# Every sentence ends.
# Max 800 chars.
pasteText: |
  First imperative.
  Second imperative.
  Third — the line someone screenshots.

# Optional. A short paragraph that lands AFTER the paste block.
# Tells the reader what to expect from the agent's response and what to do next.
# Max 400 chars.
afterPaste: |
  If the agent comes back with X, you have ground truth.
  If it comes back with Y, do Z.

# 1-3 paragraphs explaining WHY the paste works.
# Plain English. No jargon without inline definition.
# First paragraph names the mechanism. Second names the move.
# Use *single asterisks* for emphasis; they render as italics.
whyThisWorks:
  - "First paragraph: when the agent does X, it's usually because Y. Names the underlying mechanism in one sentence the reader can repeat."
  - "Second paragraph: pushing back changes the mode from *guessing* to *working*. About X% of the time, the better path emerges on the first try."

# 2-4 paragraphs, first-person scar-tissue mode ("I once X. Later Y. The lesson was Z.").
# Each paragraph carries ONE concrete image. Use *italics* sparingly for the line that lands.
# STERILIZATION: no client/project/service names. No specific dollar amounts. No date breadcrumbs.
# Check site/CLAUDE.md § Phrasebook story mode for the variance constraint — no two entries
# share the same anchoring image.
whereThisCameFrom:
  - "I once did X. Concrete first scar."
  - "Later, I learned Y. Concrete second image."
  - "The lesson was Z. *The single sentence that lodges.*"

# Publication date. Auto-coerced to Date. ISO format.
pubDate: 2026-MM-DD
---
